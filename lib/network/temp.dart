import 'package:flutter/material.dart';

class Temp extends StatelessWidget {
  static const String routeName='widgets/network/temp';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('暂无'),
      ),
    );
  }
}
